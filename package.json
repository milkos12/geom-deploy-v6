{
  "name": "mono-geom",
  "version": "0.0.1",
  "description": "Mono GEOM",
  "private": true,
  "workspaces": [
    "express-geom",
    "react-geom"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "if [ -d .git ]; then npx husky install; else echo 'Husky no instalado, omitiendo prepare'; fi"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.7.1",
    "@commitlint/config-conventional": "^19.7.1",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.8"
  },
  "lint-staged": {
    "*.js": []
  }
}
